<div class="edit-text">Editing Book</div>

<div class="error-messageh2">
<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved:
</div>
<ul>
<div class="error-message">
<% @book.errors.full_messages.each do |message| %>
<li><%= message %></li>
<% end %>
</ul>
<% end %>
</div>

<%= form_with model: @book do |f| %>
<form>
<div class="edit-text-content">Title</div>
<%=f.text_field :title %>


<div class="edit-text-content">Body</div>
<%=f.text_area :body %>

<div>
<%=f.submit 'Updete Book' %>
</div>
</form>
<% end %>

<%= link_to "Show", book_path(@book.id) %> |
<%= link_to "Back", books_path %>